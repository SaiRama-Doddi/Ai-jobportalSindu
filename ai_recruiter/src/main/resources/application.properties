spring.application.name=ai_recruiter

# --- Server ---
server.port=8081


# --- Datasource (PostgreSQL) ---
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}




#spring.datasource.url=jdbc:postgresql://ep-odd-bush-a4wqa81j-pooler.us-east-1.aws.neon.tech/job-db\?sslmode=require&channelBinding=require
#spring.datasource.username=neondb_owner
#spring.datasource.password=npg_Lz1QVypvwqT8
spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA / Hibernate ---
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


# --- Flyway ---
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# --- File upload max size (resume upload ????) ---
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# --- JWT Config ---
app.jwt.secret=${JWT_KEY}


#app.jwt.secret=2f5jylYy8xPyG5gd25iWVG8vkPqUQgXRhhqfVc4Ale0=
app.jwt.expiration-ms=86400000


# --- RSA Keys (for resume encryption) ---
app.crypto.rsa.private-key-path=classpath:keys/private_key.pem
app.crypto.rsa.public-key-path=classpath:keys/public_key.pem



spring.ai.ollama.base-url=http://localhost:11434

# Use translation-capable model
spring.ai.ollama.chat.model=qwen2.5:7b-instruct

# Stable translation
spring.ai.ollama.chat.options.temperature=0.1
spring.ai.ollama.chat.options.top-p=0.9

#ollama run phi3:mini